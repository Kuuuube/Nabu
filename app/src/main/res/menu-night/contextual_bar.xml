<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/note_pin"
        android:contentDescription="@string/pin_the_current_note"
        android:icon="?attr/pinButton"
        android:title="@string/pin"
        app:showAsAction="always" />
    <item
        android:id="@+id/note_send_to_trash"
        android:contentDescription="@string/send_the_current_note_to_trash"
        android:icon="?attr/trashButton1"
        android:title="@string/send_to_trash"
        app:showAsAction="always" />
    <item
        android:id="@+id/note_send_to_archive"
        android:contentDescription="@string/send_the_current_note_to_archive"
        android:icon="?attr/archiveButton"
        android:title="@string/send_to_archive"
        app:showAsAction="ifRoom" />
</menu>